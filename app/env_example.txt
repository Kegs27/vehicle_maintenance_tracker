# Database Configuration
# For local development, leave this empty to use SQLite
# For cloud deployment, set this to your PostgreSQL connection string
DATABASE_URL=postgresql://username:password@host:port/database_name

# App Configuration
# Set to "production" when deployed to cloud
ENVIRONMENT=development

# Secret key for production (generate a random string)
SECRET_KEY=your-secret-key-here
